<%= form_with(model: api_v1_customer, local: true) do |form| %>
  <% if api_v1_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_customer.errors.count, "error") %> prohibited this api_v1_customer from being saved:</h2>

      <ul>
        <% api_v1_customer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
