<%= form_with(model: api_v1_ride, local: true) do |form| %>
  <% if api_v1_ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_ride.errors.count, "error") %> prohibited this api_v1_ride from being saved:</h2>

      <ul>
        <% api_v1_ride.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
